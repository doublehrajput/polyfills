  interface Number {
    /**
     * Returns a string representation in Disk Size.
     * @param decimals Specifies a radix for converting numeric values to strings.
     */
    toDiskSize(decimals?: number): string;
  }
